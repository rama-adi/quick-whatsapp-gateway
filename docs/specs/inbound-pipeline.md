# Inbound Pipeline

Status: stub — filled in with the implementation.

Scope: normalize whatsmeow events into the versioned envelope, command interceptor, identity/contacts/groups capture, message/chat/poll-vote persistence, auto-read, fan-out.
