# whatsmeow Store

Status: stub — filled in with the implementation.

Scope: the keystore behind whatsmeow: SQLite fallback (modernc.org/sqlite) and the MySQL custom adapter implementing the store interfaces (wmstore_* tables).
