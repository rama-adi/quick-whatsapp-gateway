# Frontend

> ⚠️ **SUPERSEDED (v1).** This targets the embedded **React Router SPA**. v2 is a fullstack
> **TanStack Start** app with **better-auth** (org switcher, serverless-hostable, talks to the
> gateway directly). See [`../../masterplan-mvp.md`](../../masterplan-mvp.md) §12 and
> [`_V2-STATUS.md`](_V2-STATUS.md). To be rewritten in **R3**.

Status: stub — filled in with the implementation.

Scope: the React Router + shadcn SPA: admin/user/viewer/contacts surfaces, NDJSON realtime wiring, typed API client generated from openapi.yaml.
