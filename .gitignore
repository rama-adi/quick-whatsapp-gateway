# Build output
tmp/
/gateway
/router

# Frontend
web/node_modules/
web/build/
web/.output/
web/.nitro/
web/.tanstack/
web/dist/

# Local databases / keystore
*.db
store.db

# Environment / secrets (gateway env lives at deploy/.env; the `.env` glob below
# matches it in any directory)
.env

# Local dev scratch (gateway build, logs, keystore) — never committed
.dev/

# Claude Code local (personal) settings — shared config goes in .claude/settings.json
.claude/settings.local.json
.gocache