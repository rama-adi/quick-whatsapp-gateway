# Build output
tmp/
/gateway

# Frontend
web/node_modules/
web/build/
web/.output/
web/.nitro/
web/.tanstack/
web/dist/

# Local databases / keystore
*.db
store.db

# Environment / secrets
.env
deploy/gateway.dev.env

# Local dev scratch (gateway build, logs, keystore) — never committed
.dev/

# Claude Code local (personal) settings — shared config goes in .claude/settings.json
.claude/settings.local.json
