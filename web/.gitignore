.DS_Store
.env
.env.*
!.env.example
/node_modules/

# TanStack Start / Vite / Nitro build output
/.output/
/.nitro/
/.tanstack/
/dist/

# fumadocs-mdx generates this (the `collections/*` modules) on dev/build.
/.source/

# NOTE: app/routeTree.gen.ts is intentionally COMMITTED so `pnpm typecheck`
# (plain `tsc --noEmit`, no pre-generate step) works on a fresh checkout. The
# Start vite plugin regenerates it on dev/build.
