---
title: Event shape
description: The fields in every gateway event, plus a copy-pasteable JSON example for each event type and state. The same shape arrives on the realtime WebSocket and on webhooks.
---

Every event the gateway emits is one JSON object with the same outer fields. Those
outer fields are the **envelope**; the part that changes per event type sits in one
of them, `payload`. The gateway sends this same object two ways: on the realtime WebSocket
and on webhooks (more on both below).

This page lists the envelope fields, then gives a complete, copy-pasteable example for
every event type and every state it can be in. To set up a subscription, see
[Receive events](/docs/guides/receiving).

## Envelope fields

```json
{
  "schema": "v1",
  "id": "evt_01J9XYZABCDEF0123456789",
  "event": "message",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400000000,
  "payload": {}
}
```

| Field | Type | What it is |
|---|---|---|
| `schema` | string | Envelope version. Always `v1` today. If this ever changes, check it before reading the rest. |
| `id` | string | The event id: a ULID (a sortable unique id) prefixed with `evt_`. Use it to skip duplicates and to resume the stream where you left off. |
| `event` | string | The event type, one of the values in the [catalog](#event-catalog) below. It tells you how to read `payload`. |
| `session` | string | The session the event belongs to (`sess_…`). A session is one linked WhatsApp number. |
| `organization` | string | The organization that owns the session. Every event belongs to exactly one organization. |
| `timestamp` | integer | When the gateway emitted the event, in milliseconds since the Unix epoch. |
| `payload` | object | The event-specific data. Its fields depend on `event` — see each entry below. |

You get the same object whether you receive realtime events or webhooks, so you only
write your handler once. The two delivery methods differ only in how the bytes reach you:

| | Realtime WebSocket | Webhook |
|---|---|---|
| How it arrives | Open a WebSocket after minting a single-use ticket via POST /api/v1/realtime/ticket | The gateway sends an HTTP `POST` to your URL |
| Format | Discrete JSON text frames, one object per frame | The envelope as a JSON request body |
| Keepalive | A `ping` event arrives about every 20 seconds | n/a |
| Dropped connection | Mint a new ticket with `since={lastEventId}` to replay what you missed | The gateway retries the delivery |
| How to skip duplicates | Track the envelope `id` | The envelope `id` is also in the `X-Webhook-Request-Id` header |
| Authenticity | The ticket (single-use, short-lived) authorizes the socket | Each `POST` is signed: `X-Webhook-Hmac`, with `X-Webhook-Hmac-Algorithm: sha512` |

## Connect to realtime events

Realtime events use a WebSocket with a two-step handshake: mint a ticket, then open the socket.

**Step 1: Mint a ticket**

```bash
curl -X POST "https://your-gateway/api/v1/realtime/ticket" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "scope": "organization",
    "events": ["*"],
    "session": "sess_01J8ABCDEF0123456789"
  }'
```

Response:

```json
{
  "ticket": "rt_...",
  "expiresInSeconds": 30,
  "url": "wss://your-gateway/api/v1/realtime?ticket=rt_..."
}
```

**Step 2: Open the WebSocket**

Connect to the `url` from the response (or build it: take your base URL, swap `http(s)` for `ws(s)`, and append `/api/v1/realtime?ticket=rt_...`).

The ticket is single-use and expires in ~30 seconds. Mint one ticket per connection, immediately before opening the socket. Use the ticket (not your bearer token) to authorize the connection.

**Request body fields for the ticket**

- `scope` (string, optional, default `organization`) — `organization` (all sessions your org owns), `session` (one session), or `firehose` (all events).
- `session` (string, optional) — required when `scope` is `session`. The `sess_...` id.
- `events` (string array, optional, default `["*"]`) — event types to receive. Examples: `["*"]` for all, `["message", "poll.vote"]` to narrow.
- `since` (string, optional) — the `id` of the last event you processed. Use this when reconnecting to resume from where you left off. The server replays everything after this id (up to 1000 events), then tails live. Deduplicate on the `id` field.

**Over the socket**

The server sends discrete JSON text frames (not NDJSON lines):

- First frame: `{"event":"connected","heartbeatSeconds":20,"timestamp":...}`
- Heartbeat: `{"event":"ping","timestamp":...}` about every 20 seconds. If pings stop, reconnect.
- Error: `{"event":"error","error":"..."}`
- Data: the full event envelope with `schema`, `id`, `event`, `session`, `organization`, `timestamp`, `payload`.

## Event catalog

There are 17 event types, grouped below. Every example is a complete envelope you can
copy. A field that is empty is left out of the JSON entirely rather than sent as `null`
(except `media`, which is explained below). Each entry lists which of its fields are
optional or nullable.

| Event | Fires when |
|---|---|
| [`session.status`](#sessionstatus) | A session changes connection state. |
| [`auth.qr`](#authqr) | A new QR code is ready to scan. |
| [`auth.code`](#authcode) | Pairing succeeded. |
| [`message`](#message) | An inbound message arrives. |
| [`message.from_me`](#messagefrom_me) | A message is sent from this number, including from another device linked to it. |
| [`message.status`](#messagestatus) | A receipt updates a sent message. |
| [`message.reaction`](#messagereaction) | Someone reacts to a message. |
| [`message.edited`](#messageedited) | A message is edited. |
| [`message.revoked`](#messagerevoked) | A message is deleted for everyone. |
| [`poll.vote`](#pollvote) | Someone votes on a poll. |
| [`presence.update`](#presenceupdate) | A contact goes online/offline or starts typing. |
| [`group.update`](#groupupdate) | A group's metadata changes. |
| [`group.participant`](#groupparticipant) | Someone joins, leaves, or changes role. |
| [`chat.update`](#chatupdate) | A chat changes (currently profile-picture changes). |
| [`contact.update`](#contactupdate) | A contact's name or details change. |
| [`call.incoming`](#callincoming) | An incoming call. |
| [`newsletter.update`](#newsletterupdate) | A followed channel updates. |

<Callout type="info">
**Attachments are never downloaded.** When a message has an attachment, you get
`hasMedia: true` and `media: null`. So you know an attachment exists and what kind it is
(from `type`), but this release never downloads the file itself.
</Callout>

## Session & Auth

<Accordions type="single">

<Accordion title="session.status — connection state changed" id="sessionstatus">

`payload.status` is one of six values. Pick a tab to see each.

<Tabs items={["starting", "scan_qr_code", "working", "failed", "stopped", "logged_out"]}>

<Tab value="starting">

The session is connecting (or reconnecting after a transient disconnect).

```json
{
  "schema": "v1",
  "id": "evt_01J9STARTING0000000000001",
  "event": "session.status",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400000000,
  "payload": {
    "status": "starting"
  }
}
```

</Tab>

<Tab value="scan_qr_code">

The session is waiting for a QR scan to link a device. An [`auth.qr`](#authqr) event
carries the actual code to render.

```json
{
  "schema": "v1",
  "id": "evt_01J9SCANQR00000000000001",
  "event": "session.status",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400001000,
  "payload": {
    "status": "scan_qr_code"
  }
}
```

</Tab>

<Tab value="working">

The session is connected and authenticated — ready to send and receive.

```json
{
  "schema": "v1",
  "id": "evt_01J9WORKING0000000000001",
  "event": "session.status",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400002000,
  "payload": {
    "status": "working"
  }
}
```

</Tab>

<Tab value="failed">

The session failed — e.g. the stream was replaced by a newer connection elsewhere.

```json
{
  "schema": "v1",
  "id": "evt_01J9FAILED00000000000001",
  "event": "session.status",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400003000,
  "payload": {
    "status": "failed"
  }
}
```

</Tab>

<Tab value="stopped">

The session was stopped (e.g. via `:stop`). It will not reconnect until started again.

```json
{
  "schema": "v1",
  "id": "evt_01J9STOPPED0000000000001",
  "event": "session.status",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400004000,
  "payload": {
    "status": "stopped"
  }
}
```

</Tab>

<Tab value="logged_out">

The device was unlinked (logged out from the phone or via `:logout`). Re-linking needs
a fresh QR/pairing.

```json
{
  "schema": "v1",
  "id": "evt_01J9LOGGEDOUT00000000001",
  "event": "session.status",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400005000,
  "payload": {
    "status": "logged_out"
  }
}
```

</Tab>

</Tabs>

**Fields**

- `status` (string, required) — one of `starting`, `scan_qr_code`, `working`, `failed`, `stopped`, `logged_out`.

</Accordion>

<Accordion title="auth.qr — QR code ready to scan" id="authqr">

```json
{
  "schema": "v1",
  "id": "evt_01J9QRCODE00000000000001",
  "event": "auth.qr",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400000500,
  "payload": {
    "code": "2@aBcDeF1234567890==,XyZ.../9876543210=="
  }
}
```

**Fields**

- `code` (string, required) — the raw QR string. Render it as a QR image for the user to scan.

</Accordion>

<Accordion title="auth.code — pairing succeeded" id="authcode">

Sent when a device links successfully, after a QR scan or a pairing code.

Throughout these events, a **JID** is WhatsApp's address for an account or chat — for
example `6281234567890@s.whatsapp.net` for a phone number or `…@g.us` for a group. A
**LID** is an alternate, privacy-preserving address WhatsApp may use for the same account.

```json
{
  "schema": "v1",
  "id": "evt_01J9PAIRED00000000000001",
  "event": "auth.code",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400006000,
  "payload": {
    "jid": "6281234567890@s.whatsapp.net",
    "lid": "123456789012345@lid",
    "businessName": "Acme Support",
    "platform": "android"
  }
}
```

**Fields** (all optional — left out when empty)

- `jid` — the linked account's phone-number address.
- `lid` — the linked account's LID, when there is one.
- `businessName` — set only for WhatsApp Business accounts.
- `platform` — the device's platform, e.g. `android` or `iphone`.

</Accordion>

</Accordions>

## Messages

The `message` and `message.from_me` events use the same payload fields; the `type`
field tells you what kind of content it holds. `media` is **always `null`**; use
`hasMedia` to tell whether an attachment exists.

### `message`

An inbound message. Pick a tab per content type.

<Tabs items={["text", "media", "location", "contact", "poll"]}>

<Tab value="text">

A plain text message. A reply also carries `quotedMessageId`; a message with mentions
carries a `mentions` JID list (both shown here).

```json
{
  "schema": "v1",
  "id": "evt_01J9MSGTEXT0000000000001",
  "event": "message",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400010000,
  "payload": {
    "waMessageId": "3EB0A1B2C3D4E5F6A7B8",
    "chatJid": "120363012345678901@g.us",
    "senderJid": "6281234567890@s.whatsapp.net",
    "senderLid": "123456789012345@lid",
    "fromMe": false,
    "type": "text",
    "body": "@628999 are we still on for tomorrow?",
    "quotedMessageId": "3EB0FEDCBA9876543210",
    "mentions": ["628999@s.whatsapp.net"],
    "hasMedia": false,
    "media": null,
    "timestamp": 1719400010000,
    "pushName": "Alex"
  }
}
```

</Tab>

<Tab value="media">

An image, video, audio clip, document, or sticker. `type` says which one, `hasMedia` is
`true`, and `media` is still `null` (the file is not downloaded). If the message has a
caption, it is in `body`.

```json
{
  "schema": "v1",
  "id": "evt_01J9MSGMEDIA000000000001",
  "event": "message",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400011000,
  "payload": {
    "waMessageId": "3EB0A1B2C3D4E5F6A7B9",
    "chatJid": "6281234567890@s.whatsapp.net",
    "senderJid": "6281234567890@s.whatsapp.net",
    "fromMe": false,
    "type": "image",
    "body": "here's the receipt",
    "hasMedia": true,
    "media": null,
    "timestamp": 1719400011000,
    "pushName": "Alex"
  }
}
```

<Callout type="info">
`type` is one of `image`, `video`, `audio`, `document`, `sticker`. The file is never
downloaded — `media` stays `null` even though `hasMedia` is `true`.
</Callout>

</Tab>

<Tab value="location">

A shared location. The coordinates ride in a `location` object.

```json
{
  "schema": "v1",
  "id": "evt_01J9MSGLOC00000000000001",
  "event": "message",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400012000,
  "payload": {
    "waMessageId": "3EB0A1B2C3D4E5F6A7BA",
    "chatJid": "6281234567890@s.whatsapp.net",
    "senderJid": "6281234567890@s.whatsapp.net",
    "fromMe": false,
    "type": "location",
    "hasMedia": false,
    "media": null,
    "location": {
      "latitude": -6.200000,
      "longitude": 106.816666,
      "name": "Monas",
      "address": "Gambir, Jakarta Pusat"
    },
    "timestamp": 1719400012000,
    "pushName": "Alex"
  }
}
```

`location.name` and `location.address` are optional; `latitude`/`longitude` are always present.

</Tab>

<Tab value="contact">

A shared contact card. The raw vCard is kept verbatim in `contact.vcard`.

```json
{
  "schema": "v1",
  "id": "evt_01J9MSGCONTACT00000000001",
  "event": "message",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400013000,
  "payload": {
    "waMessageId": "3EB0A1B2C3D4E5F6A7BB",
    "chatJid": "6281234567890@s.whatsapp.net",
    "senderJid": "6281234567890@s.whatsapp.net",
    "fromMe": false,
    "type": "contact",
    "body": "Jamie Rivera",
    "hasMedia": false,
    "media": null,
    "contact": {
      "displayName": "Jamie Rivera",
      "vcard": "BEGIN:VCARD\nVERSION:3.0\nFN:Jamie Rivera\nTEL;type=CELL;waid=628111222333:+62 811-222-333\nEND:VCARD"
    },
    "timestamp": 1719400013000,
    "pushName": "Alex"
  }
}
```

`contact.displayName` and `contact.vcard` are both optional. `body` mirrors the display name.

</Tab>

<Tab value="poll">

A poll-creation message. The poll definition rides in a `poll` object. Votes arrive
separately as [`poll.vote`](#pollvote).

```json
{
  "schema": "v1",
  "id": "evt_01J9MSGPOLL0000000000001",
  "event": "message",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400014000,
  "payload": {
    "waMessageId": "3EB0A1B2C3D4E5F6A7BC",
    "chatJid": "120363012345678901@g.us",
    "senderJid": "6281234567890@s.whatsapp.net",
    "fromMe": false,
    "type": "poll",
    "body": "Lunch on Friday?",
    "hasMedia": false,
    "media": null,
    "poll": {
      "name": "Lunch on Friday?",
      "options": ["Pizza", "Sushi", "Salad"],
      "selectableCount": 1
    },
    "timestamp": 1719400014000,
    "pushName": "Alex"
  }
}
```

`poll.selectableCount` is the max number of options a voter may pick. `body` mirrors `poll.name`.

</Tab>

</Tabs>

**Common fields** (every `message` / `message.from_me` payload)

- `waMessageId` (string, required) — the WhatsApp message id; the target other events reference.
- `chatJid` (string, required) — the chat (DM `@s.whatsapp.net`, group `@g.us`, etc.).
- `senderJid` (string, optional) — phone-number JID of the sender.
- `senderLid` (string, optional) — sender's LID identity, when the addressing uses it.
- `fromMe` (boolean, required) — `false` for `message`; `true` for `message.from_me`.
- `type` (string, required) — `text`, `image`, `video`, `audio`, `document`, `sticker`, `location`, `contact`, `poll`.
- `body` (string, optional) — text / caption / poll name / contact display name.
- `quotedMessageId` (string, optional) — set when the message is a reply.
- `mentions` (string[], optional) — mentioned JIDs.
- `hasMedia` (boolean, required) — `true` when an attachment exists; `media` is still `null`.
- `media` (object|null) — always `null` in v1.
- `timestamp` (integer, required) — epoch-ms.
- `pushName` (string, optional) — the sender's display name.

### `message.from_me`

Same fields as [`message`](#message). You get this when a message was sent from your own
number, including from another device linked to it. The only difference is that `fromMe`
is `true`.

```json
{
  "schema": "v1",
  "id": "evt_01J9MSGFROMME00000000001",
  "event": "message.from_me",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400015000,
  "payload": {
    "waMessageId": "3EB0A1B2C3D4E5F6A7BD",
    "chatJid": "6281234567890@s.whatsapp.net",
    "senderJid": "6289876543210@s.whatsapp.net",
    "fromMe": true,
    "type": "text",
    "body": "yes, see you at noon",
    "hasMedia": false,
    "media": null,
    "timestamp": 1719400015000,
    "pushName": "You"
  }
}
```

### `message.status`

The status of one or more sent messages changed. A single update can cover several
messages at once, so `messageIds` is a list.

The `status` value comes from one of two places:

| Status | Set by |
|---|---|
| `pending`, `sent`, `failed` | The gateway, as it accepts and sends your outbound message. Not from WhatsApp. |
| `delivered`, `read`, `played` | A receipt from WhatsApp. |

<Tabs items={["pending", "sent", "delivered", "read", "played", "failed"]}>

<Tab value="pending">

Queued by the gateway, not yet acknowledged by WhatsApp (ack level 0). Set on send, not from a receipt.

```json
{
  "schema": "v1",
  "id": "evt_01J9STPENDING00000000001",
  "event": "message.status",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400016000,
  "payload": {
    "chatJid": "6281234567890@s.whatsapp.net",
    "messageIds": ["3EB0A1B2C3D4E5F6A7BD"],
    "status": "pending",
    "timestamp": 1719400016000
  }
}
```

</Tab>

<Tab value="sent">

Accepted by the WhatsApp server (ack level 1). Set on send, not from a receipt.

```json
{
  "schema": "v1",
  "id": "evt_01J9STSENT000000000000001",
  "event": "message.status",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400016500,
  "payload": {
    "chatJid": "6281234567890@s.whatsapp.net",
    "messageIds": ["3EB0A1B2C3D4E5F6A7BD"],
    "status": "sent",
    "timestamp": 1719400016500
  }
}
```

</Tab>

<Tab value="delivered">

Delivered to the recipient's device (ack level 2). Driven by a delivery receipt.

```json
{
  "schema": "v1",
  "id": "evt_01J9STDELIVERED000000001",
  "event": "message.status",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400017000,
  "payload": {
    "chatJid": "6281234567890@s.whatsapp.net",
    "senderJid": "6281234567890@s.whatsapp.net",
    "messageIds": ["3EB0A1B2C3D4E5F6A7BD"],
    "status": "delivered",
    "timestamp": 1719400017000
  }
}
```

</Tab>

<Tab value="read">

Read by the recipient (ack level 3, the blue ticks). Driven by a read receipt.

```json
{
  "schema": "v1",
  "id": "evt_01J9STREAD0000000000001",
  "event": "message.status",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400018000,
  "payload": {
    "chatJid": "6281234567890@s.whatsapp.net",
    "senderJid": "6281234567890@s.whatsapp.net",
    "messageIds": ["3EB0A1B2C3D4E5F6A7BD", "3EB0A1B2C3D4E5F6A7BE"],
    "status": "read",
    "timestamp": 1719400018000
  }
}
```

</Tab>

<Tab value="played">

A voice note / view-once media was played (ack level 4). Driven by a played receipt.

```json
{
  "schema": "v1",
  "id": "evt_01J9STPLAYED000000000001",
  "event": "message.status",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400019000,
  "payload": {
    "chatJid": "6281234567890@s.whatsapp.net",
    "senderJid": "6281234567890@s.whatsapp.net",
    "messageIds": ["3EB0A1B2C3D4E5F6A7BF"],
    "status": "played",
    "timestamp": 1719400019000
  }
}
```

</Tab>

<Tab value="failed">

The message failed to send. Set internally, not from a receipt.

```json
{
  "schema": "v1",
  "id": "evt_01J9STFAILED00000000001",
  "event": "message.status",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400019500,
  "payload": {
    "chatJid": "6281234567890@s.whatsapp.net",
    "messageIds": ["3EB0A1B2C3D4E5F6A7C0"],
    "status": "failed",
    "timestamp": 1719400019500
  }
}
```

</Tab>

</Tabs>

**Fields**

- `chatJid` (string, required) — the chat the message belongs to.
- `senderJid` (string, optional) — the JID the receipt came from (set on receipt-driven updates).
- `messageIds` (string[], required) — the `waMessageId`s this receipt updates.
- `status` (string, required) — `pending`, `sent`, `delivered`, `read`, `played`, `failed`.
- `timestamp` (integer, required) — epoch-ms.

## Reactions, edits, and deletes

These three are their own event types, but they reuse the message payload fields. In all
three, `targetId` is the `waMessageId` of the message being acted on.

<Accordions type="single">

<Accordion title="message.reaction — add or remove a reaction" id="messagereaction">

`reaction` carries the emoji; an empty string means the reaction was **removed**.

<Tabs items={["add", "remove"]}>

<Tab value="add">

```json
{
  "schema": "v1",
  "id": "evt_01J9REACTADD00000000001",
  "event": "message.reaction",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400020000,
  "payload": {
    "waMessageId": "3EB0A1B2C3D4E5F6A7C1",
    "chatJid": "120363012345678901@g.us",
    "senderJid": "6281234567890@s.whatsapp.net",
    "fromMe": false,
    "type": "reaction",
    "targetId": "3EB0A1B2C3D4E5F6A7B8",
    "reaction": "👍",
    "hasMedia": false,
    "media": null,
    "timestamp": 1719400020000,
    "pushName": "Alex"
  }
}
```

</Tab>

<Tab value="remove">

The same shape with `reaction` set to `""` — the sender removed their earlier reaction.

```json
{
  "schema": "v1",
  "id": "evt_01J9REACTDEL00000000001",
  "event": "message.reaction",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400021000,
  "payload": {
    "waMessageId": "3EB0A1B2C3D4E5F6A7C2",
    "chatJid": "120363012345678901@g.us",
    "senderJid": "6281234567890@s.whatsapp.net",
    "fromMe": false,
    "type": "reaction",
    "targetId": "3EB0A1B2C3D4E5F6A7B8",
    "reaction": "",
    "hasMedia": false,
    "media": null,
    "timestamp": 1719400021000,
    "pushName": "Alex"
  }
}
```

</Tab>

</Tabs>

**Fields**

- `targetId` (string) — the reacted-to message's `waMessageId`.
- `reaction` (string) — the emoji, or `""` when the reaction is removed.
- Plus the common message fields (`waMessageId`, `chatJid`, `senderJid`, `fromMe`, `timestamp`, `pushName`).

</Accordion>

<Accordion title="message.edited — a message was edited" id="messageedited">

`body` is the new text; `targetId` is the edited message's id.

```json
{
  "schema": "v1",
  "id": "evt_01J9EDITED00000000000001",
  "event": "message.edited",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400022000,
  "payload": {
    "waMessageId": "3EB0A1B2C3D4E5F6A7C3",
    "chatJid": "6281234567890@s.whatsapp.net",
    "senderJid": "6281234567890@s.whatsapp.net",
    "fromMe": false,
    "type": "text",
    "body": "actually, let's make it 1pm",
    "targetId": "3EB0A1B2C3D4E5F6A7B8",
    "hasMedia": false,
    "media": null,
    "timestamp": 1719400022000,
    "pushName": "Alex"
  }
}
```

**Fields**

- `targetId` (string) — the edited message's `waMessageId`.
- `body` (string) — the new text.
- An edited reply also carries `quotedMessageId`/`mentions` from the new content.

</Accordion>

<Accordion title="message.revoked — deleted for everyone" id="messagerevoked">

A delete-for-everyone. There is no body — only the target id.

```json
{
  "schema": "v1",
  "id": "evt_01J9REVOKED0000000000001",
  "event": "message.revoked",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400023000,
  "payload": {
    "waMessageId": "3EB0A1B2C3D4E5F6A7C4",
    "chatJid": "6281234567890@s.whatsapp.net",
    "senderJid": "6281234567890@s.whatsapp.net",
    "fromMe": false,
    "type": "revoke",
    "targetId": "3EB0A1B2C3D4E5F6A7B8",
    "hasMedia": false,
    "media": null,
    "timestamp": 1719400023000,
    "pushName": "Alex"
  }
}
```

**Fields**

- `targetId` (string) — the revoked message's `waMessageId`.

</Accordion>

</Accordions>

## Polls

### Poll creation

A poll being posted arrives as a regular `message` event (or `message.from_me` if this
account posted it) with `type` set to `poll`. The question and options are in the `poll`
field. The gateway records the options so it can resolve later votes back to readable
labels.

```json
{
  "schema": "v1",
  "id": "evt_01J9POLLNEW0000000000001",
  "event": "message",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400023500,
  "payload": {
    "waMessageId": "3EB0A1B2C3D4E5F6A7BC",
    "chatJid": "120363012345678901@g.us",
    "senderJid": "6281234567890@s.whatsapp.net",
    "fromMe": false,
    "type": "poll",
    "body": "Lunch?",
    "poll": {
      "name": "Lunch?",
      "options": ["Pizza", "Sushi"],
      "selectableCount": 1
    },
    "hasMedia": false,
    "media": null,
    "timestamp": 1719400023500,
    "pushName": "Alex"
  }
}
```

**Fields**

- `poll.name` (string) — the poll question.
- `poll.options` (string[]) — the answer options, in creation order.
- `poll.selectableCount` (number) — how many options a voter may pick (1 = single choice).

### `poll.vote`

Someone voted on a poll. `targetId` is the original poll message they voted on.
`selectedOptions` holds the chosen option **labels**: WhatsApp transmits a vote as
SHA-256 hashes of the options, and the gateway resolves them back to text using the
options it recorded when the poll was created. If a vote arrives for a poll the gateway
never saw (so it can't resolve a hash), that entry falls back to the raw hash string.

```json
{
  "schema": "v1",
  "id": "evt_01J9POLLVOTE0000000000001",
  "event": "poll.vote",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400024000,
  "payload": {
    "waMessageId": "3EB0A1B2C3D4E5F6A7C5",
    "chatJid": "120363012345678901@g.us",
    "senderJid": "6281234567890@s.whatsapp.net",
    "fromMe": false,
    "type": "poll_vote",
    "targetId": "3EB0A1B2C3D4E5F6A7BC",
    "selectedOptions": ["Sushi"],
    "hasMedia": false,
    "media": null,
    "timestamp": 1719400024000,
    "pushName": "Alex"
  }
}
```

**Fields**

- `targetId` (string) — the poll-creation message's `waMessageId`.
- `selectedOptions` (string[]) — the chosen option labels (falls back to the raw SHA-256 hash for any option the gateway can't resolve).
- Plus the common message fields (`waMessageId`, `chatJid`, `senderJid`, `fromMe`, `timestamp`, `pushName`).

## Presence, chats & contacts

<Accordions type="single">

<Accordion title="presence.update — online/offline or typing" id="presenceupdate">

This event covers two things. Online/offline for a contact comes with no `chatJid`.
Typing or recording inside a chat comes with a `chatJid` (and a `media` field saying
which).

<Tabs items={["available", "unavailable", "composing", "paused"]}>

<Tab value="available">

A contact came online. Account presence — no `chatJid`.

```json
{
  "schema": "v1",
  "id": "evt_01J9PRESAVAIL00000000001",
  "event": "presence.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400025000,
  "payload": {
    "from": "6281234567890@s.whatsapp.net",
    "state": "available"
  }
}
```

</Tab>

<Tab value="unavailable">

A contact went offline. `lastSeen` (epoch-ms) is present when WhatsApp shares it.

```json
{
  "schema": "v1",
  "id": "evt_01J9PRESUNAVAIL000000001",
  "event": "presence.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400026000,
  "payload": {
    "from": "6281234567890@s.whatsapp.net",
    "state": "unavailable",
    "unavailable": true,
    "lastSeen": 1719400025000
  }
}
```

</Tab>

<Tab value="composing">

The contact is typing (chat presence). `media` is `text` while typing, `audio` while
recording a voice note.

```json
{
  "schema": "v1",
  "id": "evt_01J9PRESCOMPOSING0000001",
  "event": "presence.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400027000,
  "payload": {
    "chatJid": "6281234567890@s.whatsapp.net",
    "from": "6281234567890@s.whatsapp.net",
    "state": "composing",
    "media": "text"
  }
}
```

</Tab>

<Tab value="paused">

The contact stopped typing (chat presence).

```json
{
  "schema": "v1",
  "id": "evt_01J9PRESPAUSED00000000001",
  "event": "presence.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400028000,
  "payload": {
    "chatJid": "6281234567890@s.whatsapp.net",
    "from": "6281234567890@s.whatsapp.net",
    "state": "paused"
  }
}
```

</Tab>

</Tabs>

**Fields**

- `from` (string, required) — the contact whose presence changed.
- `state` (string, required) — `available`, `unavailable`, `composing`, `paused`.
- `chatJid` (string, optional) — set for chat presence (typing/recording), absent for account presence.
- `media` (string, optional) — `text` or `audio` (typing vs recording), set with `composing`.
- `unavailable` (boolean, optional) — `true` alongside the `unavailable` state.
- `lastSeen` (integer, optional) — epoch-ms; present when WhatsApp shares last-seen.

</Accordion>

<Accordion title="chat.update — profile-picture change" id="chatupdate">

Currently only profile-picture changes. `removed: true` means the picture was cleared.

<Tabs items={["changed", "removed"]}>

<Tab value="changed">

```json
{
  "schema": "v1",
  "id": "evt_01J9CHATPIC0000000000001",
  "event": "chat.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400029000,
  "payload": {
    "chatJid": "6281234567890@s.whatsapp.net",
    "change": "picture",
    "pictureId": "1719400029"
  }
}
```

</Tab>

<Tab value="removed">

```json
{
  "schema": "v1",
  "id": "evt_01J9CHATPICDEL000000001",
  "event": "chat.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400030000,
  "payload": {
    "chatJid": "6281234567890@s.whatsapp.net",
    "change": "picture",
    "removed": true
  }
}
```

</Tab>

</Tabs>

**Fields**

- `chatJid` (string, required) — the chat whose picture changed.
- `change` (string, required) — currently always `picture`.
- `pictureId` (string, optional) — the new picture id; absent when removed.
- `removed` (boolean, optional) — `true` when the picture was cleared.

</Accordion>

<Accordion title="contact.update — name or details changed" id="contactupdate">

This event covers two things. A change to the name a contact set for themselves carries
`pushName`. A change you make to a contact in your own address book carries `fullName`
and `firstName`.

<Tabs items={["pushName", "contact"]}>

<Tab value="pushName">

```json
{
  "schema": "v1",
  "id": "evt_01J9CONTACTPN0000000001",
  "event": "contact.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400031000,
  "payload": {
    "jid": "6281234567890@s.whatsapp.net",
    "pushName": "Alex R."
  }
}
```

</Tab>

<Tab value="contact">

```json
{
  "schema": "v1",
  "id": "evt_01J9CONTACTFULL00000001",
  "event": "contact.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400032000,
  "payload": {
    "jid": "6281234567890@s.whatsapp.net",
    "fullName": "Alex Rivera",
    "firstName": "Alex"
  }
}
```

</Tab>

</Tabs>

**Fields**

- `jid` (string, required) — the contact.
- `pushName` (string, optional) — set on a push-name change.
- `fullName` / `firstName` (string, optional) — set on an address-book contact action.

</Accordion>

</Accordions>

## Groups

<Accordions type="single">

<Accordion title="group.update — metadata changed" id="groupupdate">

A group's details changed: its subject (name), description, settings, or invite link.
When the session itself joins a group, that also arrives here, with a `reason` field.

<Tabs items={["subject", "description", "settings", "inviteLink", "joined"]}>

<Tab value="subject">

```json
{
  "schema": "v1",
  "id": "evt_01J9GRPSUBJECT00000000001",
  "event": "group.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400033000,
  "payload": {
    "groupJid": "120363012345678901@g.us",
    "subject": "Weekend Trip Planning",
    "sender": "6281234567890@s.whatsapp.net",
    "timestamp": 1719400033000
  }
}
```

</Tab>

<Tab value="description">

```json
{
  "schema": "v1",
  "id": "evt_01J9GRPDESC00000000000001",
  "event": "group.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400034000,
  "payload": {
    "groupJid": "120363012345678901@g.us",
    "description": "Pin the itinerary here.",
    "sender": "6281234567890@s.whatsapp.net",
    "timestamp": 1719400034000
  }
}
```

</Tab>

<Tab value="settings">

`isAnnounce` (only admins can post) and `isLocked` (only admins can edit info) are
nullable booleans — present only when that setting changed.

```json
{
  "schema": "v1",
  "id": "evt_01J9GRPSETTINGS000000001",
  "event": "group.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400035000,
  "payload": {
    "groupJid": "120363012345678901@g.us",
    "isAnnounce": true,
    "isLocked": false,
    "sender": "6281234567890@s.whatsapp.net",
    "timestamp": 1719400035000
  }
}
```

</Tab>

<Tab value="inviteLink">

```json
{
  "schema": "v1",
  "id": "evt_01J9GRPINVITE0000000001",
  "event": "group.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400036000,
  "payload": {
    "groupJid": "120363012345678901@g.us",
    "newInviteLink": "https://chat.whatsapp.com/AbCdEf1234567890",
    "sender": "6281234567890@s.whatsapp.net",
    "timestamp": 1719400036000
  }
}
```

</Tab>

<Tab value="joined">

The session itself just joined a group. `reason` describes how (e.g. `invite`); `subject` is the group name.

```json
{
  "schema": "v1",
  "id": "evt_01J9GRPJOINED0000000001",
  "event": "group.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400037000,
  "payload": {
    "groupJid": "120363012345678901@g.us",
    "subject": "Weekend Trip Planning",
    "reason": "invite"
  }
}
```

</Tab>

</Tabs>

**Fields**

- `groupJid` (string, required) — the group.
- `subject` / `description` (string, optional) — set when that field changed.
- `isAnnounce` / `isLocked` (boolean, nullable, optional) — group settings; present only on change.
- `newInviteLink` (string, optional) — set when the invite link was reset.
- `sender` (string, optional) — who made the change.
- `reason` (string, optional) — set on a self-join.
- `timestamp` (integer, optional) — epoch-ms of the change.

</Accordion>

<Accordion title="group.participant — join / leave / promote / demote" id="groupparticipant">

A group's membership changed. The fields `join`, `leave`, `promote`, and `demote` are
each a list of the affected people. Only the field for the action that happened is
present.

<Tabs items={["join", "leave", "promote", "demote"]}>

<Tab value="join">

```json
{
  "schema": "v1",
  "id": "evt_01J9GPJOIN0000000000001",
  "event": "group.participant",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400038000,
  "payload": {
    "groupJid": "120363012345678901@g.us",
    "sender": "6281234567890@s.whatsapp.net",
    "join": ["628111222333@s.whatsapp.net", "628444555666@s.whatsapp.net"],
    "timestamp": 1719400038000
  }
}
```

</Tab>

<Tab value="leave">

```json
{
  "schema": "v1",
  "id": "evt_01J9GPLEAVE000000000001",
  "event": "group.participant",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400039000,
  "payload": {
    "groupJid": "120363012345678901@g.us",
    "sender": "6281234567890@s.whatsapp.net",
    "leave": ["628444555666@s.whatsapp.net"],
    "timestamp": 1719400039000
  }
}
```

</Tab>

<Tab value="promote">

```json
{
  "schema": "v1",
  "id": "evt_01J9GPPROMOTE00000000001",
  "event": "group.participant",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400040000,
  "payload": {
    "groupJid": "120363012345678901@g.us",
    "sender": "6281234567890@s.whatsapp.net",
    "promote": ["628111222333@s.whatsapp.net"],
    "timestamp": 1719400040000
  }
}
```

</Tab>

<Tab value="demote">

```json
{
  "schema": "v1",
  "id": "evt_01J9GPDEMOTE000000000001",
  "event": "group.participant",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400041000,
  "payload": {
    "groupJid": "120363012345678901@g.us",
    "sender": "6281234567890@s.whatsapp.net",
    "demote": ["628111222333@s.whatsapp.net"],
    "timestamp": 1719400041000
  }
}
```

</Tab>

</Tabs>

**Fields**

- `groupJid` (string, required) — the group.
- `sender` (string, optional) — who performed the change.
- `join` / `leave` / `promote` / `demote` (string[], optional) — the affected JIDs; only the relevant one is present.
- `timestamp` (integer, optional) — epoch-ms.

</Accordion>

</Accordions>

## Calls & newsletters

<Accordions type="single">

<Accordion title="call.incoming — an incoming call" id="callincoming">

`isGroup` is `false` for a one-to-one call and `true` for a group call. A group call also
carries `groupJid`.

<Tabs items={["direct", "group"]}>

<Tab value="direct">

```json
{
  "schema": "v1",
  "id": "evt_01J9CALLDIRECT0000000001",
  "event": "call.incoming",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400042000,
  "payload": {
    "callId": "CALL_A1B2C3D4E5F6",
    "from": "6281234567890@s.whatsapp.net",
    "timestamp": 1719400042000,
    "isGroup": false
  }
}
```

</Tab>

<Tab value="group">

```json
{
  "schema": "v1",
  "id": "evt_01J9CALLGROUP00000000001",
  "event": "call.incoming",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400043000,
  "payload": {
    "callId": "CALL_F6E5D4C3B2A1",
    "from": "6281234567890@s.whatsapp.net",
    "timestamp": 1719400043000,
    "isGroup": true,
    "groupJid": "120363012345678901@g.us"
  }
}
```

</Tab>

</Tabs>

**Fields**

- `callId` (string, required) — the call id.
- `from` (string, required) — the caller.
- `timestamp` (integer, required) — epoch-ms of the offer.
- `isGroup` (boolean, required) — `true` for a group call.
- `groupJid` (string, optional) — set when `isGroup` is `true`.

</Accordion>

<Accordion title="newsletter.update — a followed channel changed" id="newsletterupdate">

`action` is `join`, `leave`, or `mute`.

<Tabs items={["join", "leave", "mute"]}>

<Tab value="join">

```json
{
  "schema": "v1",
  "id": "evt_01J9NLJOIN0000000000001",
  "event": "newsletter.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400044000,
  "payload": {
    "jid": "120363098765432109@newsletter",
    "action": "join"
  }
}
```

</Tab>

<Tab value="leave">

```json
{
  "schema": "v1",
  "id": "evt_01J9NLLEAVE000000000001",
  "event": "newsletter.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400045000,
  "payload": {
    "jid": "120363098765432109@newsletter",
    "action": "leave"
  }
}
```

</Tab>

<Tab value="mute">

```json
{
  "schema": "v1",
  "id": "evt_01J9NLMUTE0000000000001",
  "event": "newsletter.update",
  "session": "sess_01J8ABCDEF0123456789",
  "organization": "org_abc",
  "timestamp": 1719400046000,
  "payload": {
    "jid": "120363098765432109@newsletter",
    "action": "mute"
  }
}
```

</Tab>

</Tabs>

**Fields**

- `jid` (string, required) — the newsletter/channel.
- `action` (string, required) — `join`, `leave`, or `mute`.

</Accordion>

</Accordions>

For the endpoints that produce and receive these events, see the
[API reference](/docs/api).
