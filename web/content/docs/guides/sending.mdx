---
title: Send a message
description: Make your first authenticated send call against a live session.
---

Once a session is paired, sending is a single `POST` to its messages endpoint
with a Bearer token. Text, polls, locations, and contacts are live; media
types arrive in a later release.

The full walkthrough — the request shape, idempotency, and async sends — is
covered here.
